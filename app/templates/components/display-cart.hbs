{{#if cartIsShowing}}
<div class="cart-products">
{{#each shoppingCart.products as |product|}}
  <p>{{product.name}}</p>
  <img class="" src="{{product.image}}" alt="" />
  <p>{{product.price}}</p>
{{/each}}
{{total}}
</div>
<button {{action 'hideCart'}}>Hide Cart</button>
{{else}}
<button {{action 'showCart'}}>View Cart</button>
{{/if}}
